{
    "name": "infinivista-backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "up": "docker compose up -d",
        "down": "docker compose down",
        "rebuild": "npm run down && docker compose build && npm run up",
        "rebuild:dev": "npm run down && docker compose build && docker compose -f docker-compose.yaml -f docker-compose.dev.yaml up -d --build ",
        "lint": "cd api-gateway && npm run lint && cd ../module-communication && npm run lint && cd ../module-feed && npm run lint && cd ../module-user && npm run lint",
        "format": "cd api-gateway && npm run format && cd ../module-communication && npm run format && cd ../module-feed && npm run format && cd ../module-user && npm run format",
        "migrate:fresh": "cd module-feed && npm run migrate:fresh && cd ../module-user && npm run migrate:fresh",
        "seed": "ts-node seed-coordinator.ts"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "@types/amqplib": "^0.10.7",
        "@types/dotenv": "^6.1.1",
        "@types/node": "^22.13.12",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.2"
    }
}
